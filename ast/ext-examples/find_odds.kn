; = XFUNC find_odds (curr, max) {
	: IF (> curr max) { X[ X] }
	: XELSE {
		; = rest XCALL(find_odds, + curr 1, max X)
		: IF (% curr 2) { X+ X[ curr X] rest }
		: XELSE { rest }
	}
}

: OUTPUT XCALL find_odds(0, 10 X)